[Unit]
Description=Reload clevo_acpi after boot (works around cold-boot DMI match failure)
After=multi-user.target
Before=restore-keyboard-color.service

[Service]
Type=oneshot
ExecStart=-/sbin/modprobe -r clevo_acpi
ExecStart=/sbin/modprobe clevo_acpi

[Install]
WantedBy=multi-user.target
